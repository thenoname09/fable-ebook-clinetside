export const metadata = {
  title: "Publish New Ebook | Fable",
  description: "Upload and publish a new ebook to the Fable platform.",

};

export default function AddEbookLayout({ children }) {
  return <>{children}</>;
}